package Tasks_5;

import Tasks_1.Point;

public class Square {
    Point start;
    int length;

    public Square(Point start, int length) {
        this.start = new Point(start.x, start.y);
        this.length = length;
    }

}
